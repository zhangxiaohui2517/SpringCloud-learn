server:
  port: 8101

spring:
  application:
    name: eureka-client
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://replica1:8002/eureka/,http://replica2:8003/eureka/ #配置中心地址
