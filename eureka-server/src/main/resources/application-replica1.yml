server:
  port: 8002
spring:
  application:
    name: eureka-server
eureka:
  instance:
    hostname: replica1
  client:
    fetch-registry: true #???????????
    register-with-eureka: true # ?????????
    service-url:
      defaultZone: http://replica2:8003/eureka/ #p??????
  server:
    enable-self-preservation: false #??????
